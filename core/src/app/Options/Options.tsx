import React from "react";
import { Button } from "antd";

interface Props {}

function Options(props: Props) {
  return (
    <>
      Options
      <Button>Click</Button>
    </>
  );
}

export default Options;
